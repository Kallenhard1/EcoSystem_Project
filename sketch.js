let ecoSystem;
let ambient;
let species;

function setup() {
  createCanvas(1920, 1080);
  ecosystem = new EcoSystem();
  ambient = new Ambient();
  aspecies = new Species();
  
}

function draw() {
  background(220);
}
